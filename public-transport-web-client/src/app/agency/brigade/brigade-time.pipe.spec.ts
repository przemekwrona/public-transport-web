import { BrigadeTimePipe } from './brigade-time.pipe';

describe('BrigadeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new BrigadeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
