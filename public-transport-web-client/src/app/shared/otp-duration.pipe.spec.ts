import { OtpDurationPipe } from './otp-duration.pipe';

describe('OtpDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new OtpDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
