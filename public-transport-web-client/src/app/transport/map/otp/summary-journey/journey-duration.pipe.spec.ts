import { JourneyDurationPipe } from './journey-duration.pipe';

describe('JourneyDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new JourneyDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
