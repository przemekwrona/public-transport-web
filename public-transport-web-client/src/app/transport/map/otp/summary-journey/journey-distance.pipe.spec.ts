import { JourneyDistancePipe } from './journey-distance.pipe';

describe('JourneyDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new JourneyDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
