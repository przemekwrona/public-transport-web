import { TimetableMinutePipe } from './timetable-minute.pipe';

describe('TimetableMinutePipe', () => {
  it('create an instance', () => {
    const pipe = new TimetableMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
