import { DepartureTimePipe } from './departure-time.pipe';

describe('DepartureTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DepartureTimePipe();
    expect(pipe).toBeTruthy();
  });
});
