import { DepartureTimePrefixPipe } from './departure-time-prefix.pipe';

describe('DepartureTimePrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartureTimePrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
