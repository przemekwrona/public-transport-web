name: public-transport

services:
  public-transport-db:
    image: postgres:18.6-alpine
    container_name: public-transport-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-public_transport_db}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - ./data/public-transport-db:/var/lib/postgresql
    # $$ keeps the expansion in the container's shell instead of compose.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s

  public-transport-app:
    # Dockerfile only copies target/public-transport-web-server-*.jar,
    # so `mvn clean package -DskipTests` in the repo root must run first.
    build:
      context: ./public-transport-web-server
      dockerfile: Dockerfile
    # image: registry.digitalocean.com/public-transport-registry/public-transport-web:latest
    container_name: public-transport-app
    restart: unless-stopped
    ports:
      - "8084:8080"
    environment:
      PROFILE: ${PROFILE:-dev}
      JAVA_OPTS: -Xmx512m -Xms256m
      SPRING_DATASOURCE_URL: jdbc:postgresql://public-transport-db:5432/${POSTGRES_DB:-public_transport_db}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-postgres}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      GEOAPIFY_KEY: ${GEOAPIFY_KEY:-}
    # Liquibase runs at startup, so Postgres has to accept connections first.
    depends_on:
      public-transport-db:
        condition: service_healthy
      keycloak:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s
    security_opt:
      - no-new-privileges:true

  keycloak-db:
    image: postgres:18.6-alpine
    container_name: keycloak-db
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: ${KEYCLOAK_DB:-keycloak}
      POSTGRES_USER: ${KEYCLOAK_DB_USER:-keycloak}
      POSTGRES_PASSWORD: ${KEYCLOAK_DB_PASSWORD:-keycloak}
    volumes:
      - ./data/keycloak-db:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s

  keycloak:
    image: quay.io/keycloak/keycloak:26.7.4
    container_name: keycloak
    restart: unless-stopped
    command: start-dev
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak-db:5432/${KEYCLOAK_DB:-keycloak}
      KC_DB_USERNAME: ${KEYCLOAK_DB_USER:-keycloak}
      KC_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD:-keycloak}
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
    ports:
      - "8081:8080"
    depends_on:
      keycloak-db:
        condition: service_healthy

  #  gtfs-warszawa:
  #    build:
  #      context: ../public-transport-otp
  #      dockerfile: Dockerfile.warszawa
  #    container_name: gtfs-warszawa
  #    ports:
  #      - '8082:8080'
  #    environment:
  #      - JAVA_OPTS=-Xmx5G

  #  otp_berlin:
  #    image: docker.io/opentripplanner/opentripplanner:latest
  #    container_name: otp_berlin
  #    ports:
  #      - '8082:8080'
  #    environment:
  #      - JAVA_OPTS=-Xmx5G
  #      -

  #  docker run -it --rm -p 8080:8080 -e JAVA_TOOL_OPTIONS='-Xmx10g' -v "$(pwd)/berlin:/var/opentripplanner" docker.io/opentripplanner/opentripplanner:latest --build --serve

#  gtfs-krakow:
#    build:
#      context: ../public-transport-otp
#      dockerfile: Dockerfile.krakow
#    container_name: gtfs-krakow
#    networks:
#      - gtfs_network
#    ports:
#      - '8084:8080'
#    environment:
#      - JAVA_OPTS=-Xmx2G
