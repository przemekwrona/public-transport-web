name: Deploy master

on: [pull_request]

jobs:
  call-build-and-test-workflow:
    name: Test
    uses: ./.github/workflows/job-test-workflow.yml@main
    with:
      artifact: public-transport-web-server
