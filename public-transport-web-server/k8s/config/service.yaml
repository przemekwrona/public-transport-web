apiVersion: v1
kind: Service
metadata:
  name: public-transport-web-server
spec:
  selector:
    app.kubernetes.io/name: public-transport-web-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
