---
# Shared by all environments. Per-environment values (certbot_domains, ansible_host, ...)
# live in inventories/<env>/group_vars/all.yml
certbot_email: przemekwrona25@gmail.com
# Use Let's Encrypt staging while testing (avoids rate limits)
certbot_staging: false
